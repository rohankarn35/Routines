import 'package:flutter/material.dart';

class Prefrencespage extends StatelessWidget {
  const Prefrencespage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("This is prefrences page"),
      ),
    );
  }
}
